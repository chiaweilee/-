const key = {
  'hotel-nights': 'hotel-nights',
  image: 'image',
  images: 'images',
  secret: 'secret',
  'bing-map': 'bing-map',
  pronounce: 'pronounce',
  'secret-image': 'secret-image',
};

const lang = Object.keys(key);

module.exports = {
  lang,
  key,
};
